# ignore everything (dirs, files, sub-dirs, sub-files)
/*
# but do not ignore the .gitignore file
!.gitignore

# do not ignore TPU dir (needed due to the above bullet point)
!/TPU
# ignore files and sub dirs in the /TPU directory
/TPU/*
# but do not ignore the .vhd file
!/TPU/*.vhd
